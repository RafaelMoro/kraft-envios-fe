import { QuoteImage } from "../types/quotes.types"

export const DEFAULT_ENVELOPE_LENGTH = 34
export const DEFAULT_ENVELOPE_HEIGHT = 27
export const DEFAULT_ENVELOPE_WIDTH = 2

export const DEFAULT_IMAGE_LOGO_PROVIDER: QuoteImage = {
  source: "/kraft-logo.svg",
  provider: "other",
  width: 100,
  height: 50
}